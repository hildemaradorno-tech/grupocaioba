node_modules
/dist
.env
.env.local
.env.local.*

# Local Netlify folder
.netlify

# Banco de dados local (KPI)
backend/portal.db

# Arquivos de credenciais avulsos
Acesso Azure.txt
*.txt.secret
Acesso*.txt

# Planilhas de referência baixadas localmente (dados de RH/financeiro, não são código)
/Arquivos

# Arquivos de exemplo (XML de notas etc.) para eu analisar — dados sensíveis, não são código
/documentosparanalise

# Cache local do Supabase CLI (project ref, pooler URL etc.)
supabase/.temp
.env.development.local
